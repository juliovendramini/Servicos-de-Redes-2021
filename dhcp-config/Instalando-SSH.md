 ## Primeiro utilizamos os comados a instalação dos pacotes do ssh : 
  sudo apt-get update
  sudo apt-get install openssh-server openssh-client
  
 ## Terminada a instalação dos pacotes utilizamos o comando ssh usuario@ip no caso da minha máquina o comando ficou como ssh user@192.168.10.100,após o comando
 havera um pedido do terminal como é o primeiro login,então digitamos yes e pronto.
 
 
